PORT=3000
SESSION_SECRET=change_me_in_production
# false = wyłącz captcha przy logowaniu
CAPTCHA_ENABLED=true

# SMTP (opcjonalnie — bez konfiguracji emaile są logowane w konsoli)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=pharmacore@apteka.local
